
  # Social Network Life Phases

  This is a code bundle for Social Network Life Phases. The original project is available at https://www.figma.com/design/CNpycRTbqRsx5UrXm1uwFI/Social-Network-Life-Phases.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  